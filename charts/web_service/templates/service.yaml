apiVersion: v1
kind: Service
metadata:
  name: {{ include "web_service.fullname" . }}
  labels:
    {{- include "web_service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web_service.selectorLabels" . | nindent 4 }}
