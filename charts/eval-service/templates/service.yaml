apiVersion: v1
kind: Service
metadata:
  name: {{ include "eval-service.fullname" . }}
  labels:
    {{- include "eval-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eval-service.selectorLabels" . | nindent 4 }}
